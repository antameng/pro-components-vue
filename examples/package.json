{
  "name": "@am/examples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@vue/tsconfig": "^0.7.0",
    "@am/packages": "workspace:*"
  }
}
